<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Sneakers</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1>All Sneakers</h1>
    <ul>
      <% allSneakers.forEach(sneaker => { %>
      <li>
        <a href="/api/sneakers/<%= sneaker.id %>"
          ><%= sneaker.name %> - <%= sneaker.brand %> - $<%= sneaker.price %></a
        >
      </li>
      <% }); %>
    </ul>
    <a href="/">Back to Home</a>
  </body>
</html>
